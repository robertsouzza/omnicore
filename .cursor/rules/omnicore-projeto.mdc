---
description: Contexto persistente do projeto OmniCore (cronograma, decisões, stack)
alwaysApply: true
---

# OmniCore — memória do projeto

Antes de implementar, leia se necessário: `.cursor/CONTEXTO-OMNICORE.md` (ou `@.cursor/CONTEXTO-OMNICORE.md`).

## Essencial

- Monorepo na raiz `~/omnicore/`: `cerebro-backend/` (Spring Boot 3.5, Java 21) + `frontend-app/` (React — em construção)
- Backend core concluído: Produto, Estoque, Vendas, ComposicaoPacote, Cliente, Colaborador, Auth JWT, CI
- Próximo foco: **14-B/C** Stone/Getnet (sandbox) → **14-D** fiscal → **Fase 15** e-commerce B2C
- Simulador pagamento: repo irmão **`~/omnicore-pagamento-simulador/`** — skill `@.cursor/skills/omnicore-pagamento-simulador/SKILL.md` (fora do monorepo git)
- Sessão 9.1: clientes estrangeiros (tipo documento) — Fase B entrega internacional = débito técnico no CONTEXTO
- Imagem produto: upload JPG/PNG/WebP via MinIO dev ✅ (11.5); produção = trocar `omnicore.storage.*` (S3/Cloudinary)
- Catálogo/precificação/compras: cadastro enxuto + preço livre; estoque separado do cadastro (Sessão 10); fornecedor/NF-e/custo/margem = débito técnico — **precificação pós-11; NF-e Fase 6**
- Frontend roadmap: **10.5** hooks ✅ · **12.5** UI kit ✅ · **13-FE** Query+Vitest ✅ · **11.5** upload imagem ✅
- Usuário: Roberto · Assistente: Logan · Idioma: português (BR)
- Não commitar `docker-compose.yml` sem pedido explícito
- Commits/push só quando Roberto pedir
- Config Cursor/Agent na raiz: `~/omnicore/.cursor/` (rules + contexto)

## Ao fechar uma sessão (obrigatório)

Antes do commit/push da sessão (ou no **mesmo commit**), atualizar:

1. `.cursor/CONTEXTO-OMNICORE.md` — tabela de sessões (status ✅, hash do commit), “Próximo passo”, progresso das fases, data/`git` no rodapé
2. **`README.md` (raiz)** — tabelas “Frontend (estado atual)”, “Progresso do projeto” e módulos da API se mudaram
3. `.cursor/rules/omnicore-projeto.mdc` — próximo foco + último commit relevante
4. `CONTEXTO-OMNICORE.md` (atalho raiz) — mensagem modelo com próxima sessão

Nunca encerrar sessão entregue sem sincronizar o contexto **e** o README.

## Auth (dev)

- Login: `POST /api/auth/login` — email `carlos.vendedor@omnicore.local`, senha `senha123`
- Swagger: http://localhost:8080/swagger-ui.html — Authorize com Bearer token

## Git (main)

Último commit relevante: `393a899` — QR Pix na tela, polish caixa e Paga no caixa
