---
description: Contexto persistente do projeto OmniCore (cronograma, decisões, stack)
alwaysApply: true
---

# OmniCore — memória do projeto

Antes de implementar, leia se necessário: `.cursor/CONTEXTO-OMNICORE.md` (ou `@.cursor/CONTEXTO-OMNICORE.md`).

## Essencial

- Monorepo na raiz `~/omnicore/`: `cerebro-backend/` (Spring Boot 3.5, Java 21) + `frontend-app/` (React — em construção)
- Backend core concluído: Produto, Estoque, Vendas, ComposicaoPacote, Cliente, Colaborador, Auth JWT, CI
- Próximo foco: **Sessão 10 — Estoque** no frontend
- Usuário: Roberto · Assistente: Logan · Idioma: português (BR)
- Não commitar `docker-compose.yml` sem pedido explícito
- Commits/push só quando Roberto pedir
- Config Cursor/Agent na raiz: `~/omnicore/.cursor/` (rules + contexto)

## Auth (dev)

- Login: `POST /api/auth/login` — email `carlos.vendedor@omnicore.local`, senha `senha123`
- Swagger: http://localhost:8080/swagger-ui.html — Authorize com Bearer token

## Git (main)

Último commit relevante: `7490a01` — Sessão 9 clientes (CRUD, CEP, telefone internacional)
